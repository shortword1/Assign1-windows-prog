<UserControl x:Class="Assign1.NowPlayingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assign1"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid Background="#AA000000">
        <!-- Semi-transparent background -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="textBlockTitle" Text="Title" Foreground="White" FontSize="16" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock x:Name="textBlockArtist" Text="Artist" Foreground="White" FontSize="14" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
